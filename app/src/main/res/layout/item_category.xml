<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="1dp">

    <!--<com.github.siyamed.shapeimageview.RoundedImageView
        android:id="@+id/ivImage"
        android:layout_width="match_parent"
        android:layout_height="170dp"
        android:src="@drawable/icon_no_image"
        app:siRadius="5dp"
        app:siBorderWidth="1px"
        app:siBorderColor="@color/color_gray"
        app:siSquare="false"/>-->

    <ImageView
        android:id="@+id/ivImage"
        android:layout_width="match_parent"
        android:layout_height="170dp"
        android:scaleType="centerCrop"
        android:src="@drawable/icon_no_image"/>

    <View
        android:layout_width="match_parent"
        android:layout_height="170dp"
        android:background="@drawable/example_cell"/>

    <TextView
        android:id="@+id/tvCatName"
        style="@style/tv_setting"
        android:gravity="center"
        android:layout_alignBottom="@+id/ivImage"
        android:textColor="@android:color/white"
        android:layout_margin="@dimen/setting_layout_view_padding_top"
        android:text="" />

</RelativeLayout>